# Web Scraping
requests==2.31.0
beautifulsoup4==4.12.3
lxml==5.1.0
# httpx is managed by supabase – do NOT pin it here to avoid version conflicts

# Browser TLS fingerprint for Indian gov PDF downloads (fixes ECONNREFUSED)
# Wraps libcurl and impersonates Chrome/Safari TLS at the handshake level.
# Without this, mrvc.indianrailways.gov.in and similar sites refuse TCP connections.
curl_cffi>=0.6.0

# Database
supabase==2.0.3

# Google Drive
google-auth==2.27.0
google-auth-oauthlib==1.2.0
google-auth-httplib2==0.2.0
google-api-python-client==2.111.0

# Utilities
python-dotenv==1.0.0
python-dateutil==2.8.2

# PDF Processing (pure Python - NO poppler binary required)
# pdfplumber: primary text extractor
# pymupdf (fitz): fallback text extractor
pdfplumber>=0.10.3
pymupdf>=1.23.0

# JSON Repair (fixes malformed LLM JSON output)
json-repair>=0.28.0

# Image support
Pillow==10.2.0

# Logging and Monitoring
coloredlogs==15.0.1

# Data Processing
pandas==2.1.4

# ===========================================
# Gemma 3 12B LLM via Ollama
# ===========================================
# Uses Ollama HTTP API - NO PYTHON PACKAGE NEEDED
# Just install Ollama and run: ollama pull gemma3:12b
#
# Setup:
# 1. Install Ollama: curl -fsSL https://ollama.com/install.sh | sh
# 2. Pull model:    ollama pull gemma3:12b
# 3. Start server:  ollama serve
